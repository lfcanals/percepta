
# Dependencies
    * Python 
    * OpenCV: `pip install opencv-python` (ETA: 60 minutes)
    * MoviePy: 'pip install moviepy`

# Fast guide : how to demo
There are three demo python files:
    * eyeFishCalibraton.py : object to callibrate the eye fish camera

The rest of Python files are:
    * use-case-eyefish-calibration.py : example of calibration using test images

